import { Text } from "react-native"
import { SettingsContainer } from "./styles"

export function Settings() {
    return (
        <SettingsContainer>
            <Text>Conta</Text>
        </SettingsContainer>
    )
}